package com.practice.java.java_4_collection.collectionInterface.setInterface;

public class HashSetClass {
    /*HashSet = Only unique values
    No duplicates allowed
    Used mainly for duplicate detection*/

    /*set.add(x);
    set.contains(x);
    set.remove(x);*/

    /*Use HashSet when:
    want to check duplicate exists
    You want unique elements only
    Example: [1,2,3,1]
    If you try to add 1 again → duplicate found.*/

}
